#include <stdio.h>

int main (void)
{	

	printf("folat的长度是: %lu \n", sizeof(float));
	printf("double的长度是: %lu \n", sizeof(double));
	printf("long double的长度是: %lu \n", sizeof(long double));

	return 0;
}