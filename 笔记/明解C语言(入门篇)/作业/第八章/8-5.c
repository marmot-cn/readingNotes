#include <stdio.h>

enum gender {Male, Famle};
enum season {Spring=1, Summer, Autumn, Winter};

int main (void)
{
	printf("male is %d\n", Male);
	printf("famale is %d\n", Famle);

	printf("Spring is %d\n", Spring);
	printf("Summer is %d\n", Summer);
	printf("Autumn is %d\n", Autumn);
	printf("Winter is %d\n", Winter);
	return 0;
}